// conditionals.cvm — if / else branching.

let x = 7;

if (x > 5)
{
    print(x);
}
else
{
    print(0);
}

// Nested conditional: print sign of (x - 5).
let y = x - 5;
if (y > 0)
{
    print(1);
}
else
{
    if (y < 0)
    {
        print(-1);
    }
    else
    {
        print(0);
    }
}
